// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: api/proto/bot/v1/bot.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "domsnail.bot.v1";

export interface BotsInfo {
  bots: BotInfo[];
}

export interface BotInfo {
}

export interface BotCredentials {
  name: string;
  apiKey?: string | undefined;
}

export interface BotConnectionInfo {
  queuePool: string;
  queuePassword: string;
}

function createBaseBotsInfo(): BotsInfo {
  return { bots: [] };
}

export const BotsInfo: MessageFns<BotsInfo> = {
  encode(message: BotsInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.bots) {
      BotInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BotsInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBotsInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bots.push(BotInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BotsInfo {
    return { bots: globalThis.Array.isArray(object?.bots) ? object.bots.map((e: any) => BotInfo.fromJSON(e)) : [] };
  },

  toJSON(message: BotsInfo): unknown {
    const obj: any = {};
    if (message.bots?.length) {
      obj.bots = message.bots.map((e) => BotInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BotsInfo>, I>>(base?: I): BotsInfo {
    return BotsInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BotsInfo>, I>>(object: I): BotsInfo {
    const message = createBaseBotsInfo();
    message.bots = object.bots?.map((e) => BotInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBotInfo(): BotInfo {
  return {};
}

export const BotInfo: MessageFns<BotInfo> = {
  encode(_: BotInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BotInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBotInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): BotInfo {
    return {};
  },

  toJSON(_: BotInfo): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<BotInfo>, I>>(base?: I): BotInfo {
    return BotInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BotInfo>, I>>(_: I): BotInfo {
    const message = createBaseBotInfo();
    return message;
  },
};

function createBaseBotCredentials(): BotCredentials {
  return { name: "", apiKey: undefined };
}

export const BotCredentials: MessageFns<BotCredentials> = {
  encode(message: BotCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.apiKey !== undefined) {
      writer.uint32(18).string(message.apiKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BotCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBotCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BotCredentials {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      apiKey: isSet(object.apiKey) ? globalThis.String(object.apiKey) : undefined,
    };
  },

  toJSON(message: BotCredentials): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.apiKey !== undefined) {
      obj.apiKey = message.apiKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BotCredentials>, I>>(base?: I): BotCredentials {
    return BotCredentials.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BotCredentials>, I>>(object: I): BotCredentials {
    const message = createBaseBotCredentials();
    message.name = object.name ?? "";
    message.apiKey = object.apiKey ?? undefined;
    return message;
  },
};

function createBaseBotConnectionInfo(): BotConnectionInfo {
  return { queuePool: "", queuePassword: "" };
}

export const BotConnectionInfo: MessageFns<BotConnectionInfo> = {
  encode(message: BotConnectionInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queuePool !== "") {
      writer.uint32(10).string(message.queuePool);
    }
    if (message.queuePassword !== "") {
      writer.uint32(18).string(message.queuePassword);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BotConnectionInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBotConnectionInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.queuePool = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.queuePassword = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BotConnectionInfo {
    return {
      queuePool: isSet(object.queuePool) ? globalThis.String(object.queuePool) : "",
      queuePassword: isSet(object.queuePassword) ? globalThis.String(object.queuePassword) : "",
    };
  },

  toJSON(message: BotConnectionInfo): unknown {
    const obj: any = {};
    if (message.queuePool !== "") {
      obj.queuePool = message.queuePool;
    }
    if (message.queuePassword !== "") {
      obj.queuePassword = message.queuePassword;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BotConnectionInfo>, I>>(base?: I): BotConnectionInfo {
    return BotConnectionInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BotConnectionInfo>, I>>(object: I): BotConnectionInfo {
    const message = createBaseBotConnectionInfo();
    message.queuePool = object.queuePool ?? "";
    message.queuePassword = object.queuePassword ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
