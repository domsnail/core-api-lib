// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: api/proto/coordinator/v1/coordinator.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "domsnail.coordinator.v1";

export enum CoordinatorMode {
  COORDINATOR_MODE_MASTER = 0,
  COORDINATOR_MODE_WORKER = 1,
  UNRECOGNIZED = -1,
}

export function coordinatorModeFromJSON(object: any): CoordinatorMode {
  switch (object) {
    case 0:
    case "COORDINATOR_MODE_MASTER":
      return CoordinatorMode.COORDINATOR_MODE_MASTER;
    case 1:
    case "COORDINATOR_MODE_WORKER":
      return CoordinatorMode.COORDINATOR_MODE_WORKER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CoordinatorMode.UNRECOGNIZED;
  }
}

export function coordinatorModeToJSON(object: CoordinatorMode): string {
  switch (object) {
    case CoordinatorMode.COORDINATOR_MODE_MASTER:
      return "COORDINATOR_MODE_MASTER";
    case CoordinatorMode.COORDINATOR_MODE_WORKER:
      return "COORDINATOR_MODE_WORKER";
    case CoordinatorMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface CoordinatorState {
  info: CoordinatorInfo | undefined;
}

export interface CoordinatorInfo {
  name: string;
  mode: CoordinatorMode;
}

function createBaseCoordinatorState(): CoordinatorState {
  return { info: undefined };
}

export const CoordinatorState: MessageFns<CoordinatorState> = {
  encode(message: CoordinatorState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.info !== undefined) {
      CoordinatorInfo.encode(message.info, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoordinatorState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoordinatorState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.info = CoordinatorInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoordinatorState {
    return { info: isSet(object.info) ? CoordinatorInfo.fromJSON(object.info) : undefined };
  },

  toJSON(message: CoordinatorState): unknown {
    const obj: any = {};
    if (message.info !== undefined) {
      obj.info = CoordinatorInfo.toJSON(message.info);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoordinatorState>, I>>(base?: I): CoordinatorState {
    return CoordinatorState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoordinatorState>, I>>(object: I): CoordinatorState {
    const message = createBaseCoordinatorState();
    message.info = (object.info !== undefined && object.info !== null)
      ? CoordinatorInfo.fromPartial(object.info)
      : undefined;
    return message;
  },
};

function createBaseCoordinatorInfo(): CoordinatorInfo {
  return { name: "", mode: 0 };
}

export const CoordinatorInfo: MessageFns<CoordinatorInfo> = {
  encode(message: CoordinatorInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.mode !== 0) {
      writer.uint32(16).int32(message.mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoordinatorInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoordinatorInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoordinatorInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      mode: isSet(object.mode) ? coordinatorModeFromJSON(object.mode) : 0,
    };
  },

  toJSON(message: CoordinatorInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.mode !== 0) {
      obj.mode = coordinatorModeToJSON(message.mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoordinatorInfo>, I>>(base?: I): CoordinatorInfo {
    return CoordinatorInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoordinatorInfo>, I>>(object: I): CoordinatorInfo {
    const message = createBaseCoordinatorInfo();
    message.name = object.name ?? "";
    message.mode = object.mode ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
