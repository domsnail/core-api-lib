// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: api/proto/probe/v1/probe.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ProbeOptions, WhoisInfo } from "../../whois/v1/whois";

export const protobufPackage = "domsnail.coordinator.v1";

export enum ProbeType {
  PROBE_TYPE_UNKNOWN = 0,
  PROBE_TYPE_WHOIS = 1,
  PROBE_TYPE_PING = 2,
  UNRECOGNIZED = -1,
}

export function probeTypeFromJSON(object: any): ProbeType {
  switch (object) {
    case 0:
    case "PROBE_TYPE_UNKNOWN":
      return ProbeType.PROBE_TYPE_UNKNOWN;
    case 1:
    case "PROBE_TYPE_WHOIS":
      return ProbeType.PROBE_TYPE_WHOIS;
    case 2:
    case "PROBE_TYPE_PING":
      return ProbeType.PROBE_TYPE_PING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProbeType.UNRECOGNIZED;
  }
}

export function probeTypeToJSON(object: ProbeType): string {
  switch (object) {
    case ProbeType.PROBE_TYPE_UNKNOWN:
      return "PROBE_TYPE_UNKNOWN";
    case ProbeType.PROBE_TYPE_WHOIS:
      return "PROBE_TYPE_WHOIS";
    case ProbeType.PROBE_TYPE_PING:
      return "PROBE_TYPE_PING";
    case ProbeType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GenericProbe {
  UUID: string;
  type: ProbeType;
  responseRequired: boolean;
  whois?: ProbeOptions | undefined;
}

export interface GenericProbes {
  probes: GenericProbe[];
}

export interface GenericProbeResponse {
  UUID: string;
  whois?: WhoisInfo | undefined;
}

function createBaseGenericProbe(): GenericProbe {
  return { UUID: "", type: 0, responseRequired: false, whois: undefined };
}

export const GenericProbe: MessageFns<GenericProbe> = {
  encode(message: GenericProbe, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.UUID !== "") {
      writer.uint32(10).string(message.UUID);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.responseRequired !== false) {
      writer.uint32(24).bool(message.responseRequired);
    }
    if (message.whois !== undefined) {
      ProbeOptions.encode(message.whois, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenericProbe {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenericProbe();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.UUID = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.responseRequired = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.whois = ProbeOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenericProbe {
    return {
      UUID: isSet(object.UUID) ? globalThis.String(object.UUID) : "",
      type: isSet(object.type) ? probeTypeFromJSON(object.type) : 0,
      responseRequired: isSet(object.responseRequired) ? globalThis.Boolean(object.responseRequired) : false,
      whois: isSet(object.whois) ? ProbeOptions.fromJSON(object.whois) : undefined,
    };
  },

  toJSON(message: GenericProbe): unknown {
    const obj: any = {};
    if (message.UUID !== "") {
      obj.UUID = message.UUID;
    }
    if (message.type !== 0) {
      obj.type = probeTypeToJSON(message.type);
    }
    if (message.responseRequired !== false) {
      obj.responseRequired = message.responseRequired;
    }
    if (message.whois !== undefined) {
      obj.whois = ProbeOptions.toJSON(message.whois);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenericProbe>, I>>(base?: I): GenericProbe {
    return GenericProbe.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenericProbe>, I>>(object: I): GenericProbe {
    const message = createBaseGenericProbe();
    message.UUID = object.UUID ?? "";
    message.type = object.type ?? 0;
    message.responseRequired = object.responseRequired ?? false;
    message.whois = (object.whois !== undefined && object.whois !== null)
      ? ProbeOptions.fromPartial(object.whois)
      : undefined;
    return message;
  },
};

function createBaseGenericProbes(): GenericProbes {
  return { probes: [] };
}

export const GenericProbes: MessageFns<GenericProbes> = {
  encode(message: GenericProbes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.probes) {
      GenericProbe.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenericProbes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenericProbes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.probes.push(GenericProbe.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenericProbes {
    return {
      probes: globalThis.Array.isArray(object?.probes) ? object.probes.map((e: any) => GenericProbe.fromJSON(e)) : [],
    };
  },

  toJSON(message: GenericProbes): unknown {
    const obj: any = {};
    if (message.probes?.length) {
      obj.probes = message.probes.map((e) => GenericProbe.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenericProbes>, I>>(base?: I): GenericProbes {
    return GenericProbes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenericProbes>, I>>(object: I): GenericProbes {
    const message = createBaseGenericProbes();
    message.probes = object.probes?.map((e) => GenericProbe.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGenericProbeResponse(): GenericProbeResponse {
  return { UUID: "", whois: undefined };
}

export const GenericProbeResponse: MessageFns<GenericProbeResponse> = {
  encode(message: GenericProbeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.UUID !== "") {
      writer.uint32(10).string(message.UUID);
    }
    if (message.whois !== undefined) {
      WhoisInfo.encode(message.whois, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenericProbeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenericProbeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.UUID = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.whois = WhoisInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenericProbeResponse {
    return {
      UUID: isSet(object.UUID) ? globalThis.String(object.UUID) : "",
      whois: isSet(object.whois) ? WhoisInfo.fromJSON(object.whois) : undefined,
    };
  },

  toJSON(message: GenericProbeResponse): unknown {
    const obj: any = {};
    if (message.UUID !== "") {
      obj.UUID = message.UUID;
    }
    if (message.whois !== undefined) {
      obj.whois = WhoisInfo.toJSON(message.whois);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenericProbeResponse>, I>>(base?: I): GenericProbeResponse {
    return GenericProbeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenericProbeResponse>, I>>(object: I): GenericProbeResponse {
    const message = createBaseGenericProbeResponse();
    message.UUID = object.UUID ?? "";
    message.whois = (object.whois !== undefined && object.whois !== null)
      ? WhoisInfo.fromPartial(object.whois)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
