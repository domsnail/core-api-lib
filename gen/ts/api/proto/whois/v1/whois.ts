// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: api/proto/whois/v1/whois.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { Host } from "../../host/v1/host";

export const protobufPackage = "domsnail.whois.v1";

export interface ProbeOptions {
  host: Host | undefined;
}

export interface WhoisInfo {
  host: string;
  rawData: string;
  sourceServer: string;
  /** redirected is used to link to data sourced from original whois server */
  redirected?: WhoisInfo | undefined;
  createdAt: Date | undefined;
}

function createBaseProbeOptions(): ProbeOptions {
  return { host: undefined };
}

export const ProbeOptions: MessageFns<ProbeOptions> = {
  encode(message: ProbeOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== undefined) {
      Host.encode(message.host, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProbeOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProbeOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = Host.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProbeOptions {
    return { host: isSet(object.host) ? Host.fromJSON(object.host) : undefined };
  },

  toJSON(message: ProbeOptions): unknown {
    const obj: any = {};
    if (message.host !== undefined) {
      obj.host = Host.toJSON(message.host);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProbeOptions>, I>>(base?: I): ProbeOptions {
    return ProbeOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProbeOptions>, I>>(object: I): ProbeOptions {
    const message = createBaseProbeOptions();
    message.host = (object.host !== undefined && object.host !== null) ? Host.fromPartial(object.host) : undefined;
    return message;
  },
};

function createBaseWhoisInfo(): WhoisInfo {
  return { host: "", rawData: "", sourceServer: "", redirected: undefined, createdAt: undefined };
}

export const WhoisInfo: MessageFns<WhoisInfo> = {
  encode(message: WhoisInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== "") {
      writer.uint32(10).string(message.host);
    }
    if (message.rawData !== "") {
      writer.uint32(18).string(message.rawData);
    }
    if (message.sourceServer !== "") {
      writer.uint32(26).string(message.sourceServer);
    }
    if (message.redirected !== undefined) {
      WhoisInfo.encode(message.redirected, writer.uint32(42).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WhoisInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWhoisInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rawData = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourceServer = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.redirected = WhoisInfo.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WhoisInfo {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      rawData: isSet(object.rawData) ? globalThis.String(object.rawData) : "",
      sourceServer: isSet(object.sourceServer) ? globalThis.String(object.sourceServer) : "",
      redirected: isSet(object.redirected) ? WhoisInfo.fromJSON(object.redirected) : undefined,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
    };
  },

  toJSON(message: WhoisInfo): unknown {
    const obj: any = {};
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.rawData !== "") {
      obj.rawData = message.rawData;
    }
    if (message.sourceServer !== "") {
      obj.sourceServer = message.sourceServer;
    }
    if (message.redirected !== undefined) {
      obj.redirected = WhoisInfo.toJSON(message.redirected);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WhoisInfo>, I>>(base?: I): WhoisInfo {
    return WhoisInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WhoisInfo>, I>>(object: I): WhoisInfo {
    const message = createBaseWhoisInfo();
    message.host = object.host ?? "";
    message.rawData = object.rawData ?? "";
    message.sourceServer = object.sourceServer ?? "";
    message.redirected = (object.redirected !== undefined && object.redirected !== null)
      ? WhoisInfo.fromPartial(object.redirected)
      : undefined;
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
